{
  "name": "hlnode-mcp",
  "version": "0.1.0",
  "description": "Read-only MCP server for a Hyperliquid node's local info server",
  "license": "MIT",
  "type": "module",
  "bin": { "hlnode-mcp": "dist/index.js" },
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@types/node": "24.13.6",
    "typescript": "5.9.3",
    "vitest": "3.2.7"
  }
}
